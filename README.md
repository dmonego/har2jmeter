har2jmeter.py
=============

A tool for converting .har (Http ARchive) to jMeter load tests. 

Currently only tested on har files from chrome.

Requirements
------------

Requires jinja2. Install it using either of these:

```easy_install Jinja2
pip install Jinja2```

